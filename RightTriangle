import turtle

board = turtle.Turtle()

board.forward(100)  # draw base

board.left(90)
board.forward(100)

board.left(135)
board.forward(141.4213562373095)

turtle.done()

#Uses Turtle to load a graphic page, that then plots the triangle with a 90 degree angle.
